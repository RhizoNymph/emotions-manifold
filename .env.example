# Copy to .env and fill in. .env is gitignored.

# vLLM server — leave blank if the local fork does not require auth.
VLLM_API_KEY=
VLLM_STEERING_API_KEY=

# Anthropic API key for the LLM judge.
ANTHROPIC_API_KEY=
